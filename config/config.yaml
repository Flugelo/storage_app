# app/config/config.yml
doctrine:
  orm:
    # ...
    entity_managers:
      default:
        # ...
        dql:
          datetime_functions:
            date:        'Mapado\MysqlDoctrineFunctions\DQL\MysqlDate'
            date_format: 'Mapado\MysqlDoctrineFunctions\DQL\MysqlDateFormat'
          # ... add all functions you need

lexik_jwt_authentication:
  # outras configurações...
  # ...

  # ativa o debug da biblioteca
  debug: '%kernel.debug%'

monolog:
  handlers:
    main:
      # outras configurações...
      # ...

      # ajusta o nível de log para exibir mensagens de debug do Lexik JWT Authentication Bundle
      level: debug
      channels: ["security"]